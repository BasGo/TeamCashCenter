version: '3.8'
services:
  app:
    build: .
    ports:
      - "5005:80"
    volumes:
      - ./TeamCashCenter.db:/app/TeamCashCenter.db
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    restart: unless-stopped
